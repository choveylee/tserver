// Package tmiddleware provides Gin middleware for common cross-cutting HTTP concerns,
// including CORS, rate limiting, and request-body buffering for scenarios that require
// the body to be read more than once.
package tmiddleware
